# Postgres (use your local PG or docker-compose service)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/civicbalance

# Data paths (set to your actual CSV filenames under ./data)
# GoEmotions (choose ONE mirror)
GOEMOTIONS_CSV=data/goemotions_shivamb.csv
# or: GOEMOTIONS_CSV=data/goemotions_debarshi.csv

# Jigsaw multilingual toxic (train CSV)
JIGSAW_TRAIN_CSV=data/jigsaw_multilingual_train.csv

# Optional: extra positive dataset (Twitter emotions)
TWITTER_EMOTION_CSV=

# Artifacts
ARTIFACT_DIR=artifacts

# App
API_URL=http://localhost:8000/predict